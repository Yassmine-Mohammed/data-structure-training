using System;

namespace datastructurebasics
{
    public class Program
    {
        static void Main(string[] args)
        {
            /* 
            Stack : 1) Array based implementation of the ADT stack ------------------------------------------------

            Stack stack = new Stack();
            stack.Push(1);          // 1
            stack.Push(2);          // 2
            stack.Push(3);          // 3
            stack.Print();
            stack.GetTheTop();

            int theDeletedElement = 0;
            stack.Pop(ref theDeletedElement); // remove 3
            Console.WriteLine("the top has been deleted sucessfully");
            stack.Print();
            stack.GetTheTop();
            */

        }
    }
}
